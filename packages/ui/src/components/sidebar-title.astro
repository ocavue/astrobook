---
import '@astrobook/types'

import { getPathWithBase } from '@astrobook/core/client'
import config from 'virtual:astrobook/global-config.mjs'
---

<a
  class:list={[
    ':uno: rounded p-2 font-semibold text-gray-500 no-underline transition hover:text-gray-800 dark:text-gray-400 dark:hover:text-gray-200 line-height-[1.15]',
    'astrobook-focus-ring',
  ]}
  href={getPathWithBase('/', config.baseUrl)}
>
  {config.title}
</a>
