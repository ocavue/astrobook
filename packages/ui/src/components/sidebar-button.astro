---
interface Props {
  id?: string
  href?: string
  title: string
}

const Element = Astro.props.href ? 'a' : 'button'
---

<Element
  id={Astro.props.id}
  href={Astro.props.href}
  class:list={['astrobook-sidebar-button']}
  title={Astro.props.title}
>
  <span class=":uno: sr-only">{Astro.props.title}</span>
  <slot />
</Element>
