---
interface Props {
  id?: string
  href?: string
  title: string
}

const Element = Astro.props.href ? 'a' : 'button'
---

<Element
  id={Astro.props.id}
  href={Astro.props.href}
  class="astrobook-sidebar-button astrobook-focus-ring"
  title={Astro.props.title}
>
  <span class="astrobook-visually-hidden">{Astro.props.title}</span>
  <slot />
</Element>
